1. Identify next month
2. Find golfers who are in town
3. Confirm they have weekly play days
4. Check if they have zero daily entries for next month
5. Auto‑populate days matching their weekly play days
This ensures:

1st — initialize the new month

14th — catch late entries before the Two‑Week Report crosses months

Last day — final cleanup

update schedule
   set is_playing = 1
 where strftime('%w', date) in
       (select user_play_days.day_of_week
	      from user_play_days
	     where user_play_days.is_play_day = 1
	       and user_play_days.user_id in	 
(select distinct user_play_months.user_id 
   from user_play_months
  where user_play_months.month = ? -- parm passed to js function  
    and user_play_months.in_town = 1
    and not EXISTS 
        (select 1
           from schedule
 		  where schedule.sched_id = user_play_months.user_id
            and schedule.date >= ? -- parm passed to js function 
		    and schedule.date <  ?  -- parm passed to js function
		    and schedule.is_playing = 1)
)